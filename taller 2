#include <iostream>
#include <cstdlib> // 
#include <ctime> //

using namespace std;

struct Conjunto {
    int tamano;
    int max_valor;
    int *numeros;
};





int main() {
   int m;
   cout <<"ingrese cantidad: ";
   cin >> m;
    int* array = randomarray(m); //llamar funcion con un tamaño m

    // Imprimir arreglo generado
    for (int i = 0; i < 10; i++) {
        cout << array[i] << " ";
    }
    cout << endl;
    
      cout << "Menu:" << endl;
        cout << "1. Generar conjunto de numeros aleatorios" << endl;
        cout << "2. Mostrar histograma del conjunto" << endl;
        cout << "3. Salir" << endl;
        cout << "Ingrese una opcion: ";
        cin >> int op;
        
          switch(op) {
          
            case 1:
            break;
            case 2:
            break;
            case 3:
            break;
    return 0;
}

int* randomarray(int n) {
    
    int* arr =  int[n];

    // para crear los valores aleatorios del 0 al 20
    srand(time(NULL));
    for (int i = 0; i < n; i++) {
        array[i] = rand() % 21;
    }

    return array;
}

